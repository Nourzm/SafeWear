# Note: local builds on the dev machine fail with a daemon loopback error
# (antivirus interference) regardless of these settings; CI builds the APK.
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G
android.useAndroidX=true
kotlin.incremental=false
